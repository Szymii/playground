import { Meta } from "@storybook/react";

import { Refs } from "./Refs";

export default {
  title: "Concept/Refs",
  component: Refs,
} as Meta;

export const Default = () => {
  return (
    <>
      <Refs />
    </>
  );
};
